#/bin/ksh

# delete -fdefault-integer-8 and call gfortran on what is left, assuming gfortran is in the same directory

for arg do
  shift
  [ "$arg" = "-fdefault-integer-8" ] && continue
  set -- "$@" "$arg"
done

"${0%/*}/gfortran" "$@"
